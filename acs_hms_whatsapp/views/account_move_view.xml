<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Add WhatsApp button to Invoice Form -->
    <record id="view_account_move_form_whatsapp" model="ir.ui.view">
        <field name="name">account.move.form.whatsapp</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_invoice_sent'][1]" position="after">
                <button name="action_send_whatsapp_invoice"
                        string="Send via WhatsApp"
                        type="object"
                        class="oe_highlight"
                        invisible="move_type not in ('out_invoice', 'out_refund') or state != 'posted'"/>
            </xpath>
        </field>
    </record>

</odoo>
