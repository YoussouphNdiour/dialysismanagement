<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Liste des bilans -->
    <template id="portal_bilans" name="Portail — Mes bilans">
        <t t-call="portal.portal_layout">
            <t t-set="page_name" t-value="'bilans'"/>
            <div class="container py-4">
                    <h2 class="mb-3">Mes bilans biologiques</h2>

                    <!-- Graphiques évolution 6 mois -->
                    <t t-if="bilans">
                        <script src="/acs_hms_nephrology_portal/static/src/lib/chart.umd.min.js" type="text/javascript"/>
                        <div class="nephro-card mb-4">
                            <h5 class="mb-3">Évolution — 6 derniers bilans</h5>
                            <div class="row g-3">
                                <div class="col-12 col-md-4">
                                    <p class="text-center small text-muted mb-1">Hémoglobine (g/dL)</p>
                                    <canvas id="chartHemoglobin" height="140"/>
                                </div>
                                <div class="col-12 col-md-4">
                                    <p class="text-center small text-muted mb-1">Potassium (mmol/L)</p>
                                    <canvas id="chartPotassium" height="140"/>
                                </div>
                                <div class="col-12 col-md-4">
                                    <p class="text-center small text-muted mb-1">Phosphore (mmol/L)</p>
                                    <canvas id="chartPhosphorus" height="140"/>
                                </div>
                            </div>
                            <p class="text-muted small mt-2 mb-0">
                                <span style="border-bottom:2px dashed #aaa; display:inline-block; width:20px; vertical-align:middle;"/>
                                Lignes pointillées = zones cibles KDIGO
                            </p>
                        </div>
                        <div id="nephroChartData" t-att-data-chart="chart_data" style="display:none"/>
                        <script type="text/javascript">
                            (function() {
                                var raw = document.getElementById('nephroChartData');
                                if (!raw) return;
                                var data = JSON.parse(raw.dataset.chart);
                                if (!data || !data.labels || !data.labels.length) return;

                                var n = data.labels.length;

                                /* Ligne KDIGO horizontale (pointillés) */
                                function refLine(value, color, label) {
                                    return {
                                        label: label,
                                        data: Array(n).fill(value),
                                        borderColor: color,
                                        borderDash: [6, 4],
                                        borderWidth: 1.5,
                                        pointRadius: 0,
                                        fill: false,
                                        tension: 0,
                                    };
                                }

                                function makeChart(id, key, color, refs) {
                                    var ctx = document.getElementById(id);
                                    if (!ctx) return;
                                    var datasets = [{
                                        label: key,
                                        data: data[key],
                                        borderColor: color,
                                        backgroundColor: color + '22',
                                        tension: 0.3,
                                        fill: true,
                                        pointRadius: 4,
                                        borderWidth: 2,
                                    }];
                                    refs.forEach(function(r) { datasets.push(r); });
                                    new Chart(ctx, {
                                        type: 'line',
                                        data: { labels: data.labels, datasets: datasets },
                                        options: {
                                            responsive: true,
                                            plugins: {
                                                legend: { display: false },
                                                tooltip: {
                                                    callbacks: {
                                                        label: function(ctx) {
                                                            return ctx.dataset.label + ': ' + ctx.parsed.y;
                                                        }
                                                    }
                                                }
                                            },
                                            scales: { y: { beginAtZero: false } }
                                        }
                                    });
                                }

                                /* Hb : cible KDIGO 10–12 g/dL */
                                makeChart('chartHemoglobin', 'hemoglobin', '#e74c3c', [
                                    refLine(10, '#e74c3c88', 'Min 10'),
                                    refLine(12, '#e74c3c88', 'Max 12'),
                                ]);
                                /* K : cible 3.5–5.5 mmol/L */
                                makeChart('chartPotassium', 'potassium', '#3498db', [
                                    refLine(3.5, '#3498db88', 'Min 3.5'),
                                    refLine(5.5, '#3498db88', 'Max 5.5'),
                                ]);
                                /* P : cible 1.1–1.8 mmol/L */
                                makeChart('chartPhosphorus', 'phosphorus', '#2ecc71', [
                                    refLine(1.1, '#2ecc7188', 'Min 1.1'),
                                    refLine(1.8, '#2ecc7188', 'Max 1.8'),
                                ]);
                            })();
                        </script>
                    </t>

                    <!-- Tableau bilans -->
                    <t t-if="not bilans">
                        <p class="text-muted">Aucun bilan enregistré.</p>
                    </t>
                    <t t-if="bilans">
                        <div class="table-responsive">
                            <table class="table nephro-table-responsive">
                                <thead class="table-light">
                                    <tr>
                                        <th>Date</th>
                                        <th>Type</th>
                                        <th>Hb (g/dL)</th>
                                        <th>K (mmol/L)</th>
                                        <th>P (mmol/L)</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="bilans" t-as="b">
                                        <tr>
                                            <td data-label="Date">
                                                <t t-esc="b.exam_date and b.exam_date.strftime('%d/%m/%Y')"/>
                                            </td>
                                            <td data-label="Type">
                                                <t t-esc="b.bilan_type or '—'"/>
                                            </td>
                                            <td data-label="Hb">
                                                <t t-esc="'%.1f' % (b.hemoglobin or 0)"/>
                                            </td>
                                            <td data-label="K">
                                                <t t-esc="'%.2f' % (b.potassium or 0)"/>
                                            </td>
                                            <td data-label="P">
                                                <t t-esc="'%.2f' % (b.phosphorus or 0)"/>
                                            </td>
                                            <td>
                                                <a t-attf-href="/my/bilans/#{b.id}"
                                                   class="btn btn-sm btn-outline-secondary">
                                                    Détail
                                                </a>
                                            </td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                        </div>
                        <t t-call="portal.pager" t-if="pager"/>
                    </t>
                </div>
        </t>
    </template>

    <!-- Détail bilan -->
    <template id="portal_bilan_detail" name="Portail — Détail bilan">
        <t t-call="portal.portal_layout">
            <t t-set="page_name" t-value="'bilans'"/>
            <div class="container py-4">
                    <a href="/my/bilans" class="btn btn-sm btn-outline-secondary mb-3">
                        ← Retour aux bilans
                    </a>
                    <h2 class="mb-1">
                        Bilan du <t t-esc="bilan.exam_date and bilan.exam_date.strftime('%d/%m/%Y')"/>
                    </h2>

                    <div class="nephro-card mt-3">
                        <h5>Hématologie</h5>
                        <dl class="row">
                            <dt class="col-7">Hémoglobine</dt>
                            <dd class="col-5"><t t-esc="'%.1f g/dL' % (bilan.hemoglobin or 0)"/></dd>
                            <dt class="col-7">Ferritine</dt>
                            <dd class="col-5"><t t-esc="'%.1f µg/L' % (bilan.ferritin or 0)"/></dd>
                        </dl>
                    </div>

                    <div class="nephro-card mt-3">
                        <h5>Biochimie</h5>
                        <dl class="row">
                            <dt class="col-7">Potassium</dt>
                            <dd class="col-5"><t t-esc="'%.2f mmol/L' % (bilan.potassium or 0)"/></dd>
                            <dt class="col-7">Phosphore</dt>
                            <dd class="col-5"><t t-esc="'%.2f mmol/L' % (bilan.phosphorus or 0)"/></dd>
                            <dt class="col-7">Calcium</dt>
                            <dd class="col-5"><t t-esc="'%.2f mmol/L' % (bilan.calcium or 0)"/></dd>
                        </dl>
                    </div>

                    <t t-if="bilan.notes">
                        <div class="nephro-card mt-3">
                            <h5>Notes</h5>
                            <p><t t-esc="bilan.notes"/></p>
                        </div>
                    </t>
                </div>
        </t>
    </template>
</odoo>
